[project]
name = "dye-vat"
version = "0.1.0"
description = "Dye workshop pigment management system"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
timeout = 30
